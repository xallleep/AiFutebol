[build-system]
requires = ["setuptools==65.5.1", "wheel==0.38.4"]
build-backend = "setuptools.build_meta"
[tool.setuptools]
# Otimizações para builds no Render
zip-safe = false
include-package-data = true